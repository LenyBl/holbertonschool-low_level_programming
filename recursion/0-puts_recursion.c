#include "main.h"
/**
 * _print_recursion - puts une chaîne de caractères en utilisant récursion
 *
 * @s: pointeur vers la chaîne de caractères à imprimer
 *
 * Return: rien
 */
void _print_recursion(char *s)
{
	_puts(s);
}
